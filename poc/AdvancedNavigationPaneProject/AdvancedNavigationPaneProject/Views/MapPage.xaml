<Page
    x:Class="AdvancedNavigationPaneProject.Views.MapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d">

    <Grid>
        <maps:MapControl
            x:Name="mapControl"
            ZoomLevel="{x:Bind ViewModel.ZoomLevel, Mode=OneWay}"
            Center="{x:Bind ViewModel.Center, Mode=OneWay}"/>
        <AppBarButton Label="Expand" Icon="FullScreen" Command="{x:Bind ViewModel.ExpandCommand}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        <TickBar/>
    </Grid>
</Page>
