<MenuItem Header="{x:Static strings:Resources.ShellMenuViewsHeader}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}">
<!--^^-->
<!--{[{-->
    <MenuItem Header="{x:Static strings:Resources.ShellMenuItemViewswts.ItemNamePageHeader}" Command="{Binding MenuViewswts.ItemNameCommand}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}" />
<!--}]}-->
</MenuItem>
