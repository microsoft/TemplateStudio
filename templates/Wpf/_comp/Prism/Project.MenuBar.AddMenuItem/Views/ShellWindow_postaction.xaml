<MenuItem Header="{x:Static properties:Resources.ShellMenuViewsHeader}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}">
<!--^^-->
<!--{[{-->
    <MenuItem Header="{x:Static properties:Resources.ShellMenuItemViewswts.ItemNamePageHeader}" Command="{Binding MenuViewswts.ItemNameCommand}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}" />
<!--}]}-->
</MenuItem>
