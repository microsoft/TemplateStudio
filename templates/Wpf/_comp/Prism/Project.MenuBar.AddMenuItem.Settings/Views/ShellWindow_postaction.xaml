<controls:MetroWindow
    <controls:MetroWindow.Content>
        <Grid>
            <Menu>
                <MenuItem Header="{x:Static strings:Resources.ShellMenuFileHeader}">
<!--{[{-->
                    <MenuItem Header="{x:Static strings:Resources.ShellMenuItemFilewts.ItemNamePageHeader}" Command="{Binding MenuFilewts.ItemNameCommand}" />
                    <Separator />
<!--}]}-->
                </MenuItem>
            </Menu>
        </Grid>
    </controls:MetroWindow.Content>
</controls:MetroWindow>
