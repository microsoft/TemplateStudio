<UserControl x:Class="Param_ItemNamespace.Controls.CameraControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Microsoft.Toolkit.Uwp.UI.Converters"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/CameraPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <CaptureElement Name="PreviewControl"
                  Stretch="UniformToFill"/>

        <TextBlock x:Name="errorMessage"
                FontSize="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        
        <Button
            Content="&#xE114;"
            IsEnabled="{x:Bind IsInitialized, Mode=OneWay}"
            Style="{StaticResource CameraButtonStyle}"/>

        <Button        
        Content="&#xE124;"
        Visibility="{x:Bind CanSwitch, Converter={StaticResource BoolToVisibilityConverter}}"
        Style="{StaticResource SwitchCameraButtonStyle}"/>
    </Grid>
</UserControl>
