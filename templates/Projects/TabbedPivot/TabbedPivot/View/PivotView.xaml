<Page
    x:Class="uct.TabbedPivotProject.View.PivotView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:uct.TabbedPivotProject.Model"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot x:Uid="PivotView"  ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
            <Pivot.HeaderTemplate>
                <DataTemplate x:DataType="model:PivotTabbedItem">
                    <TextBlock Text="{x:Bind Title}"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.ItemTemplate>
                <DataTemplate x:DataType="model:PivotTabbedItem">
                    <Frame Content="{x:Bind Page}"/>
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>
    </Grid>
</Page>
