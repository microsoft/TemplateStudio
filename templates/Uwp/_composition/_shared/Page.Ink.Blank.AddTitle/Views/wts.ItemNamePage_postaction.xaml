        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="visualStateGroup">
                <VisualState x:Name="SmallVisualState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    <!--{[{-->
                        <Setter Target="headerTitle.Visibility" Value="Collapsed" />
                    <!--}]}-->
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="BigVisualState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    <!--{[{-->
                        <Setter Target="headerTitle.Visibility" Value="Visible" />
                    <!--}]}-->
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition MinWidth="384" Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!--{[{-->
            <TextBlock x:Name="headerTitle" x:Uid="wts.ItemNameHeader" Margin="{StaticResource SmallLeftMargin}" Style="{StaticResource PageTitleStyle}" MaxLines="1" MaxWidth="180" TextTrimming="CharacterEllipsis" />
            <!--}]}-->
        </Grid>