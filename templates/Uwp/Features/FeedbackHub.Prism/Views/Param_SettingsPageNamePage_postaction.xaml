<Page
    mc:Ignorable="d">
    <Grid Margin="{StaticResource MediumLeftRightMargin}">
        <StackPanel
            x:Name="ContentArea">
            <StackPanel Margin="{StaticResource ExtraSmallTopMargin}">
                <!--^^-->
                <!--{[{-->

                <HyperlinkButton
                    Margin="{StaticResource ExtraSmallTopMargin}"
                    x:Uid="FeedbackHub_Link"
                    Command="{x:Bind ViewModel.LaunchFeedbackHubCommand}"
                    Visibility="{x:Bind ViewModel.FeedbackLinkVisibility}" />
                <!--}]}-->
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
