<UserControl
    x:Class="Param_ItemNamespace.Views.MasterDetailViewDetail.MasterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Height="64" Padding="0,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <FontIcon Grid.Column="0" Tag="{x:Bind ViewModel.Item.HashIdentIcon}" FontSize="40" Glyph="{x:Bind ViewModel.Item.SymbolAsChar}" VerticalAlignment="Center" />
        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind ViewModel.Item.Company}" Tag="{x:Bind ViewModel.Item.HashIdentTitle}" Style="{ThemeResource ListTitleStyle}"/>
            <TextBlock Text="{x:Bind ViewModel.Item.Status}" Style="{StaticResource ListSubTitleStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
