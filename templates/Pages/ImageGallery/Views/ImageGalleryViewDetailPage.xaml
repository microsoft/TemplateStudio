<Page
    x:Class="Param_ItemNamespace.Views.ImageGalleryViewDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Param_ItemNamespace.Models"
    mc:Ignorable="d">
    <Grid>
        <FlipView
            x:Name="flipView"
            Visibility="{x:Bind ViewModel.FlipViewVisibility, Mode=OneWay}"
            ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedImage, Mode=TwoWay}">
            <FlipView.ItemTemplate>
                <DataTemplate x:DataType="models:SampleImage">
                    <Image
                        x:Name="detailImage"
                        Width="600" Height="600"
                        Source="{x:Bind Source, Mode=OneWay}" />
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>


        <Grid Visibility="{x:Bind ViewModel.ShapeVisibility, Mode=OneWay}" Background="{ThemeResource FlipViewBackground}"/>
        <Image
            x:Name="previewImage"
            Style="{StaticResource DetailImageStyle}"
            Visibility="{x:Bind ViewModel.PreViewImageVisibility, Mode=OneWay}"
            Source="{x:Bind ViewModel.SelectedImage.(models:SampleImage.Source), Mode=OneWay}" />
    </Grid>
</Page>
