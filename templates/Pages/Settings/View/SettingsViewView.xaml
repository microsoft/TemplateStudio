<Page
    x:Class="ItemNamespace.View.SettingsViewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d">
    <Page.Resources>
        <Thickness x:Key="ContainerMargin">24,12</Thickness>
        <Thickness x:Key="LargeTopMargin">0,16,0,0</Thickness>
        <Thickness x:Key="MediumTopMargin">0,8,0,0</Thickness>
        <Thickness x:Key="SmallTopMargin">0,4,0,0</Thickness>
    </Page.Resources>

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="{StaticResource ContainerMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                x:Uid="SettingsView_Title"
                Style="{ThemeResource HeaderTextBlockStyle}"/>

            <StackPanel Grid.Row="1" Margin="{StaticResource LargeTopMargin}">
                <TextBlock
                    x:Uid="SettingsView_Theme"
                    Style="{ThemeResource TitleTextBlockStyle}"/>
                <ToggleSwitch
                    x:Uid="SettingsView_Toggle"
                    IsOn="{x:Bind ViewModel.IsLightThemeEnabled, Mode=TwoWay}"
                    Margin="{StaticResource SmallTopMargin}">
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Toggled">
                            <core:InvokeCommandAction Command="{x:Bind ViewModel.SwitchThemeCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                </ToggleSwitch>
            </StackPanel>
            
            <StackPanel Grid.Row="2" Margin="{StaticResource LargeTopMargin}">
                <TextBlock
                    x:Uid="SettingsView_About"
                    Style="{ThemeResource TitleTextBlockStyle}"/>
                <TextBlock
                    Text="{x:Bind ViewModel.AppDescription, Mode=OneWay}"
                    Style="{ThemeResource SubtitleTextBlockStyle}"
                    Margin="{StaticResource SmallTopMargin}"/>
                <TextBlock
                    x:Uid="SettingsView_AboutDescription"
                    Style="{ThemeResource BodyTextBlockStyle}"/>
                <HyperlinkButton
                    x:Uid="SettingsView_PrivacyTermsLink"
                    Margin="{StaticResource MediumTopMargin}"/>
            </StackPanel>
            
        </Grid>
    </Grid>
</Page>