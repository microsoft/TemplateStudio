<Page
    x:Class="CaliburnMicroApp.Dessert.List.DessertListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CaliburnMicroApp.DessertList"
    xmlns:cm="using:Caliburn.Micro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Style="{StaticResource PageContainerStyle}" cm:Message.Attach="[Event Loaded] = [LoadDataAsync()]">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock x:Uid="MainTitle" Style="{StaticResource PageTitleStyle}"></TextBlock>

        <ListView Grid.Row="1" ItemsSource="{Binding DessertList}" SelectionMode="Single" IsItemClickEnabled="True" cm:Message.Attach="[Event ItemClick] = [NavigateToDetail($eventArgs)]">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <RelativePanel Style="{StaticResource ItemContainerStyle}">
                        <Image x:Name="Image" Source="{Binding Picture}" RelativePanel.AlignLeftWithPanel="True" Style="{StaticResource ThumbnailStyle}" />
                        <TextBlock x:Name="Title" Text="{Binding Title}" RelativePanel.RightOf="Image" Style="{StaticResource ItemTitleStyle}" />
                        <TextBlock Text="{Binding Category}" RelativePanel.RightOf="Image" RelativePanel.Below="Title" Style="{StaticResource ItemCaptionStyle}"/>
                    </RelativePanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
