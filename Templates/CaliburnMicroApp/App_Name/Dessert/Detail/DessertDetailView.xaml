<Page
    x:Class="App_Name.Dessert.Detail.DessertDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App_Name.Dessert.Detail"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Style="{StaticResource PageContainerStyle}">
        <StackPanel>
            <RelativePanel Style="{StaticResource ItemContainerStyle}">
                <Image x:Name="Image" Source="{Binding Parameter.Picture}" RelativePanel.AlignLeftWithPanel="True" Style="{StaticResource ThumbnailStyle}" />
                <TextBlock x:Name="Title" Text="{Binding Parameter.Title}" RelativePanel.RightOf="Image" Style="{StaticResource ItemTitleStyle}" />
                <TextBlock Text="{Binding Parameter.Category}" RelativePanel.RightOf="Image" RelativePanel.Below="Title" Style="{StaticResource ItemCaptionStyle}" />
            </RelativePanel>
            <TextBlock Text="{Binding Parameter.Subtitle}" Style="{StaticResource ItemSubTitleStyle}"></TextBlock>
            <TextBlock Text="{Binding Parameter.Description}" Style="{StaticResource ItemContentStyle}"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
