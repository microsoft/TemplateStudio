<Page
    x:Class="App_Name.App_Page.App_PagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{Binding App_PageViewModel, Source={StaticResource Locator}}">

        <i:Interaction.Behaviors>
            <core:EventTriggerBehavior EventName="Loaded">
                <core:InvokeCommandAction Command="{Binding InitializeCommand}"/>
            </core:EventTriggerBehavior>
        </i:Interaction.Behaviors>

        <TextBlock Text="{Binding PageTitle}" Style="{StaticResource PageTitleStyle}" />
    </Grid>
</Page>