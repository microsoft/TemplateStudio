<Page
    Style="{StaticResource PageStyle}">
    <ScrollViewer>
        <StackPanel
            MaxWidth="650">
<!--{[{-->
            <TextBlock x:Uid="wts.ItemName_Title" Style="{StaticResource PageTitleStyle}" />
<!--}]}-->
        </StackPanel>
    </ScrollViewer>
</Page>
