<MenuItem Header="{x:Static properties:Resources.ShellMenuViewsHeader}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}">
<!--^^-->
<!--{[{-->
    <MenuItem Header="{x:Static properties:Resources.ShellMenuItemViewsts.ItemNamePageHeader}" Command="{Binding MenuViewsts.ItemNameCommand}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}" />
<!--}]}-->
</MenuItem>
