<MenuItem Header="{x:Static properties:Resources.ShellMenuFileHeader}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}">
<!--{[{-->
    <MenuItem Header="{x:Static properties:Resources.ShellMenuItemFilets.ItemNamePageHeader}" Click="OnMenuFilets.ItemName" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}" />
    <Separator />
<!--}]}-->
</MenuItem>
