<MenuItem Header="{x:Static properties:Resources.ShellMenuViewsHeader}" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}">
<!--^^-->
<!--{[{-->
    <MenuItem Header="{x:Static properties:Resources.ShellMenuItemViewsts.ItemNamePageHeader}" Click="OnMenuViewsts.ItemName" AutomationProperties.Name="{Binding Header, RelativeSource={RelativeSource Self}}" />
<!--}]}-->
</MenuItem>
