<Window x:Class="Microsoft.Templates.VsEmulator.Main.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:Microsoft.Templates.VsEmulator.Converters"
        mc:Ignorable="d"
        KeyDown="OnKeyDown"
        Title="Visual Studio Emulator" 
        Height="480" Width="800" 
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Margin="12,12,12,0">
                    <TextBlock>
                        <Hyperlink x:Name="newCSharpProjectButton" Command="{Binding NewUwpCSharpProjectCommand}">
                            <TextBlock Text="New Uwp C# project..."/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock>
                        <Hyperlink Command="{Binding NewUwpVisualBasicProjectCommand}">
                            <TextBlock Text="New Uwp VB project..."/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock>
                        <Hyperlink Command="{Binding LoadProjectCommand}">
                            <TextBlock Text="Load project..."/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock>
                        <Hyperlink Command="{Binding AnalyzeCSharpSelectionCommand}">
                            <TextBlock Text="Analyze C# project selection"/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock>
                        <Hyperlink Command="{Binding AnalyzeVisualBasicSelectionCommand}">
                            <TextBlock Text="Analyze VB project selection"/>
                        </Hyperlink>
                    </TextBlock>
                    <ComboBox SelectedItem="{Binding SelectedTheme, Mode=TwoWay}" ItemsSource="{Binding Themes}" Margin="0,12,0,0" Style="{StaticResource VSEmulatorComboBox}"/>
                    <TextBlock Margin="0,20,0,0">
                        <Hyperlink Command="{Binding ConfigureVersionsCommand}">
                            <TextBlock Text="Configure versions..."/>
                        </Hyperlink>
                    </TextBlock>
                    <Button Command="{Binding RefreshTemplateCacheCommand}" Content="Refresh Cache" Style="{StaticResource TextButton}" Margin="0,8,0,0" ToolTip="Refresh template cache"/>
                </StackPanel>
                <ScrollViewer x:Name="logScroll" Margin="0,8" Padding="12,0" Grid.Row="1" VerticalAlignment="Bottom" VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding Log}" TextWrapping="Wrap"></TextBlock>
                </ScrollViewer>
                <StackPanel Orientation="Horizontal" Grid.Row="2" VerticalAlignment="Bottom" Margin="12,0,12,12">
                    <TextBlock Text="Wizard Version: "></TextBlock>
                    <TextBlock Text="{Binding WizardVersion}"></TextBlock>
                    <TextBlock Margin="10,0,0,0" Text="Templates Version: "></TextBlock>
                    <TextBlock Text="{Binding TemplatesVersion}"></TextBlock>
                </StackPanel>
            </Grid>
            <Grid Grid.Column="1" Style="{StaticResource VerticalSeparator}" Margin="0" />
            <ScrollViewer Grid.Column="2">
                <ItemsControl
                    Margin="18,12,12,12"
                    ItemsSource="{Binding Projects}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" Style="{StaticResource HorizontalSeparator}" />
                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding ProjectName}" Style="{StaticResource Header2}" />
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="Platform: " Style="{StaticResource Label1}" />
                                        <TextBlock Text="{Binding Platform}" Style="{StaticResource Label1}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="Language: " Style="{StaticResource Label1}" />
                                        <TextBlock Text="{Binding Language}" Style="{StaticResource Label1}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="Project Type: " Style="{StaticResource Label1}" />
                                        <TextBlock Text="{Binding ProjectType}" Style="{StaticResource Label1}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="Framework: " Style="{StaticResource Label1}" />
                                        <TextBlock Text="{Binding Framework}" Style="{StaticResource Label1}" />
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Command="{Binding OpenInVsCommand}" Style="{StaticResource IconButton}">
                                        <Button.ToolTip>Open in Visual Studio</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/vs.jpg" />
                                    </Button>
                                    <Button Command="{Binding OpenInVsCodeCommand}" Style="{StaticResource IconButton}">
                                        <Button.ToolTip>Open in Visual Studio Code</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/vscode.jpg" />
                                    </Button>
                                    <Button Command="{Binding OpenInExplorerCommand}" Style="{StaticResource IconButton}">
                                        <Button.ToolTip>Open in folder</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/folder.png" />
                                    </Button>
                                    <Button Command="{Binding AddNewPageCommand}" Style="{StaticResource IconButton}" Visibility="{Binding IsWtsProject}">
                                        <Button.ToolTip>Add new page</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/newpage.png" />
                                    </Button>
                                    <Button Command="{Binding AddNewFeatureCommand}" Style="{StaticResource IconButton}"  Visibility="{Binding IsWtsProject}">
                                        <Button.ToolTip>Add new feature</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/newfeature.png" />
                                    </Button>
                                    <Button Command="{Binding AddTestingCommand}" Style="{StaticResource IconButton}"  Visibility="{Binding StyleCopButtonVisibility}">
                                        <Button.ToolTip>Add stylecop</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/stylecop.png" />
                                    </Button>
                                    <Button Command="{Binding OpenTempInExplorerCommand}" Style="{StaticResource IconButton}" Visibility="{Binding TempFolderAvailable}">
                                        <Button.ToolTip>Open temp folder in explorer</Button.ToolTip>
                                        <Image Source="/VsEmulator;component/Assets/openfolder.png"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="1" Style="{StaticResource StatusContainer}">
            <TextBlock Text="{Binding State}" Style="{StaticResource StatusText}" />
        </Grid>
    </Grid>
</Window>
