<Window x:Class="Microsoft.Templates.VsEmulator.ProjectConfirm.ProjectConfirmationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Project created" 
        Height="220" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding SolutionName, StringFormat=The project -{0}- has been created successfully!!}" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12" TextWrapping="Wrap" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding OpenInVsCommand}" Content="Open in VS" Margin="6,0,0,0" Width="96" Height="22" />
            <Button Command="{Binding OpenInExplorerCommand}" Content="Open in Explorer" Margin="6,0,0,0" Width="96" Height="22" />
            <Button Command="{Binding CloseCommand}" Content="Close" Margin="6,0,0,0" Width="96" Height="22" />
        </StackPanel>
    </Grid>
</Window>
