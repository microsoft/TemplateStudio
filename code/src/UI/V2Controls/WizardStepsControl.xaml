<UserControl x:Class="Microsoft.Templates.UI.V2Controls.WizardStepsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Microsoft.Templates.UI.V2Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel>

        <Grid x:Name="projectConfigurationGrid" Background="LightBlue">
            <TextBlock Text="Project configuration"/>
        </Grid>
        <Grid x:Name="projectConfigurationGrid" Background="LightBlue">
            <TextBlock Text="Project configuration"/>
        </Grid>
        <Grid x:Name="projectConfigurationGrid" Background="LightBlue">
            <TextBlock Text="Project configuration"/>
        </Grid>
        <Grid x:Name="projectConfigurationGrid" Background="LightBlue">
            <TextBlock Text="Project configuration"/>
        </Grid>
        
    </StackPanel>

</UserControl>
