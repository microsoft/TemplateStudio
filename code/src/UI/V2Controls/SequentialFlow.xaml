<UserControl x:Class="Microsoft.Templates.UI.V2Controls.SequentialFlow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Microsoft.Templates.UI;component/V2Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Margin="{StaticResource Margin_L_LeftTopRightBottom}">

        <Grid>
            <Grid x:Name="projectTypeGrid" Style="{StaticResource WtsGridSequentialFlowActive}"/>
            <TextBlock x:Name="projectTypeTextBlock" Text="1. Project type" Style="{StaticResource WtsTextBlockSequentialFlowSelected}" />
        </Grid>
        
        <Grid>
            <Grid x:Name="designPatternGrid" Style="{StaticResource WtsGridSequentialFlowInactive}"/>
            <TextBlock x:Name="designPatternTextBlock" Text="2. Design pattern" Style="{StaticResource WtsTextBlockSequentialFlow}" />
        </Grid>
        
        <Grid>
            <Grid x:Name="pagesGrid" Style="{StaticResource WtsGridSequentialFlowInactive}"/>
            <TextBlock x:Name="pagesTextBlock" Text="3. Pages" Style="{StaticResource WtsTextBlockSequentialFlow}" />
        </Grid>

        <Grid>
            <Grid x:Name="featuresGrid" Style="{StaticResource WtsGridSequentialFlowInactive}"/>
            <TextBlock x:Name="featuresTextBlock" Text="4. Features" Style="{StaticResource WtsTextBlockSequentialFlow}" />
        </Grid>
        
    </StackPanel>

</UserControl>
