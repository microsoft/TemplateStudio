<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:services="clr-namespace:Microsoft.Templates.UI.V2Services">

    <Style TargetType="Hyperlink" x:Key="WtsHyperlink">
        <Setter Property="TextDecorations" Value="{x:Null}" />
        <Style.Triggers>
            <!--<Trigger Property="IsPressed" Value="true">
                <Setter Property="Foreground" Value="{Binding HyperlinkPressed, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>-->
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="{Binding HyperlinkDisabled, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
