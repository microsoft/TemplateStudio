<Window x:Class="Microsoft.Templates.UI.Views.NewItem.ProjectConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Microsoft.Templates.UI.Views.NewItem"
        mc:Ignorable="d"
        Title="ProjectConfigurationWindow" Height="300" Width="300">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/UIStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Project Type: "></TextBlock>
            <ComboBox 
                ItemsSource="{Binding ProjectTypes}"
                SelectedItem="{Binding ProjectType, Mode=TwoWay}"
                >
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="Framework: "></TextBlock>
            <ComboBox 
                ItemsSource="{Binding Frameworks}"
                SelectedItem="{Binding Framework, Mode=TwoWay}">
            </ComboBox>
        </StackPanel>
        <Grid Grid.Row="3" Grid.ColumnSpan="2">
      
            <StackPanel Orientation="Horizontal" Grid.Column="1" >
                <Button Content="Ok" Command="{Binding OkCommand}"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
