<Window x:Class="Microsoft.Templates.UI.Views.NewItem.NewItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add new Item" Height="600" Width="450"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize" 
        ShowInTaskbar="False" >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/UIStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="{StaticResource MediumLeftTopRightMargin}" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="{StaticResource MediumTopMargin}">
            <TextBlock Text="Project Type: "></TextBlock>
            <ComboBox 
                ItemsSource="{Binding ProjectTypes}"
                SelectedItem="{Binding ContextProjectType, Mode=TwoWay}"
                IsEnabled="{Binding EnableContextSelection}"
                >
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="{StaticResource MediumTopMargin}">
            <TextBlock Text="Framework: "></TextBlock>
            <ComboBox 
                ItemsSource="{Binding Frameworks}"
                SelectedItem="{Binding ContextFramework, Mode=TwoWay}"
                IsEnabled="{Binding EnableContextSelection}">
            </ComboBox>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="2" Margin="{StaticResource MediumTopMargin}">
            <TextBlock Text="Select page or framework: "></TextBlock>
            <ListView    
                SelectionMode="Single"
                Width="420"
                ItemsSource="{Binding Templates}"
                SelectedItem="{Binding TemplateSelected, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <Grid Grid.Row="3" Grid.ColumnSpan="2" Margin="{StaticResource MediumTopMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Margin="{StaticResource SmallMargin}">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Templates.Count}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding TemplateSelected.GetMultipleInstance}" Value="false">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Text="Page name:" />
                <TextBox Text="{Binding ItemName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" MaxLength="30"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" >
                <Button Content="Ok" Command="{Binding OkCommand}"  Margin="{StaticResource SmallMargin}"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="{StaticResource SmallMargin}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
