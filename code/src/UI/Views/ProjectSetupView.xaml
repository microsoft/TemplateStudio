<Page x:Class="Microsoft.Templates.UI.Views.ProjectSetupView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"      
      mc:Ignorable="d"
      Title="ProjectSetupView">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/UIStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <ScrollViewer>
            <StackPanel>
                <GroupBox Header="{Binding ProjectTypesHeader}" Margin="{StaticResource SmallMargin}">
                    <ListView
                        SelectionMode="Single"
                        Margin="{StaticResource MediumLeftTopMargin}"                        
                        Style="{StaticResource ListViewProjectSetupStyle}"
                        ItemsSource="{Binding ProjectTypes}"
                        ItemTemplate="{StaticResource ProjectSetupItemTemplate}"
                        SelectedItem="{Binding SelectedProjectType, Mode=TwoWay}" />
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
