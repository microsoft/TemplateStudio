<Window
    x:Class="Microsoft.Templates.UI.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:Microsoft.Templates.UI.Resources"    
    mc:Ignorable="d"
    Title="Windows Template Studio"
    Width="1294"
    Height="727"
    MinWidth="800"
    MinHeight="600"
    WindowStartupLocation="CenterScreen" 
    ResizeMode="CanResize" 
    ShowInTaskbar="False">
    
    <!--1294 is 1280 (real) + 14
    727 is 720 (real) + 7-->

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/UIStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Style="{StaticResource GridTitleStyle}">
            <TextBlock Style="{StaticResource TextTitleStyle}" Text="{Binding Title}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Frame x:Name="stepFrame" NavigationUIVisibility="Hidden"/>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="{x:Static res:StringRes.SummaryTitle}" />
                <Expander Grid.Row="1" Header="{x:Static res:StringRes.SummaryLicenseTitle}">
                    
                </Expander>
                <Expander Grid.Row="2" Header="{x:Static res:StringRes.SummaryProjectTypeTitle}" DataContext="{Binding ProjectSetup.SelectedProjectType}">
                    <StackPanel>
                        <TextBlock Text="{Binding DisplayName}" />
                        <TextBlock>
                            <TextBlock.Text>
                                <MultiBinding StringFormat="Made by {0}">
                                    <Binding Path="Author" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Expander>
                <Expander Grid.Row="3" Header="{x:Static res:StringRes.SummaryFrameworkTitle}" DataContext="{Binding ProjectSetup.SelectedFramework}">
                    <StackPanel>
                        <TextBlock Text="{Binding DisplayName}" />
                        <TextBlock>
                            <TextBlock.Text>
                                <MultiBinding StringFormat="Made by {0}">
                                    <Binding Path="Author" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Expander>
                <Expander Grid.Row="4" Header="{x:Static res:StringRes.SummaryPagesTitle}">

                </Expander>
                <Expander Grid.Row="5" Header="{x:Static res:StringRes.SummaryFeaturesTitle}">

                </Expander>
                <TextBlock Grid.Row="6" Text="{x:Static res:StringRes.SummaryTerms}" TextWrapping="Wrap" />
            </Grid>
        </Grid>
        <Grid Grid.Row="2" Style="{StaticResource GridBottomBarStyle}">
            <StackPanel Style="{StaticResource StackPanelBottomBarStyle}">
                <Button Style="{StaticResource ButtonNavigationStyle}" Content="{x:Static res:StringRes.ButtonCancel}" Command="{Binding CancelCommand}" />
                <Button Style="{StaticResource ButtonNavigationStyle}" Content="{x:Static res:StringRes.ButtonBack}" Command="{Binding BackCommand}" />
                <Button Style="{StaticResource ButtonNavigationStyle}" Content="{x:Static res:StringRes.ButtonNext}" Command="{Binding NextCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
