<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Microsoft.Templates.UI.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/_Colors.xaml" />
        <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/_Thickness.xaml" />
        <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/StyleTextBlock.xaml" />
        <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/StyleButton.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ItemTemplateBorderBase" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource UIWhite}" />
        <Setter Property="Height" Value="84" />
        <Setter Property="Width" Value="436" />
    </Style>

    <DataTemplate x:Key="ProjectSetupItemTemplate">
        <Border Style="{StaticResource ItemTemplateBorderBase}">
            <Grid Margin="{StaticResource MediumMargin}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <controls:ImageEx
                    Grid.RowSpan="3"
                    Focusable="False"
                    SourcePath="{Binding Icon}"/>
                <TextBlock Grid.Column="1" Style="{StaticResource TextTemplateTitleStyle}" Text="{Binding DisplayName}"/>
                <Button Grid.Column="2" Style="{StaticResource TemplateInfoButtonStyle}">
                    <Button.Content>
                        <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="18"/>
                    </Button.Content>
                </Button>
                <TextBlock Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Style="{StaticResource TextTemplateSubtitleStyle}" Text="{Binding Description}"/>
                <TextBlock Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" Style="{StaticResource TextTemplateAuthorStyle}" Text="{Binding Author}" />
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>