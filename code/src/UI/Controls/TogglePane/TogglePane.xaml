<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Microsoft.Templates.UI.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Microsoft.Templates.UI;component/Styles/UIStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Storyboard x:Key="FadeInStoryboard">
        <ColorAnimation Storyboard.TargetName="mainGrid" Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)" To="{StaticResource UILightGrayColor}" Duration="0:0:0.5" />
        <DoubleAnimation Duration="0:0:0.5" To="1" Storyboard.TargetName="openCloseButton" Storyboard.TargetProperty="Opacity"/>
    </Storyboard>

    <Storyboard x:Key="FadeOutStoryboard">
        <ColorAnimation Storyboard.TargetName="mainGrid" Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)" To="Transparent" Duration="0:0:0" />
        <DoubleAnimation Duration="0:0:0" To="0" Storyboard.TargetName="openCloseButton" Storyboard.TargetProperty="Opacity"/>
    </Storyboard>

    <Style TargetType="{x:Type controls:TogglePane}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:TogglePane}">
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Control.MouseEnter">
                            <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Control.MouseLeave">
                            <BeginStoryboard Storyboard="{StaticResource FadeOutStoryboard}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Keyboard.GotKeyboardFocus">
                            <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Keyboard.LostKeyboardFocus">
                            <BeginStoryboard Storyboard="{StaticResource FadeOutStoryboard}"/>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                    <Grid x:Name="mainGrid" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter x:Name="mainViewContentTemplate" ContentTemplate="{TemplateBinding MainViewTemplate}" Content="{TemplateBinding TogglePaneContent}"/>
                        <Grid Grid.Column="1" Background="LightCyan">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="openCloseButton" ContentTemplate="{TemplateBinding ButtonTemplate}" Content="{TemplateBinding TogglePaneContent}" Opacity="0"/>
                            <ContentPresenter Grid.Column="1" ContentTemplate="{TemplateBinding SecondaryViewTemplate}" Content="{TemplateBinding TogglePaneContent}" />
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>